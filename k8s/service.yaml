apiVersion: v1
kind: Service
metadata:
  name: eks-node-service

spec:
  selector:
    app: eks-node-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer